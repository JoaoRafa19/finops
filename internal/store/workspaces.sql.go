// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: workspaces.sql

package store

import (
	"context"
)

const createWorkspace = `-- name: CreateWorkspace :one
INSERT INTO workspaces (
    owner_user_id,
    name,
    default_currency
) VALUES (
    $1, $2, $3
)
RETURNING id, owner_user_id, name, default_currency, created_at
`

type CreateWorkspaceParams struct {
	OwnerUserID     int64
	Name            string
	DefaultCurrency string
}

func (q *Queries) CreateWorkspace(ctx context.Context, arg CreateWorkspaceParams) (Workspace, error) {
	row := q.db.QueryRowContext(ctx, createWorkspace, arg.OwnerUserID, arg.Name, arg.DefaultCurrency)
	var i Workspace
	err := row.Scan(
		&i.ID,
		&i.OwnerUserID,
		&i.Name,
		&i.DefaultCurrency,
		&i.CreatedAt,
	)
	return i, err
}

const getWorkSpaceByOwnerUserID = `-- name: GetWorkSpaceByOwnerUserID :one
SELECT
    id,
    owner_user_id,
    name,
    default_currency,
    created_at
FROM 
    workspaces 
WHERE owner_user_id = $1
LIMIT 1
`

func (q *Queries) GetWorkSpaceByOwnerUserID(ctx context.Context, ownerUserID int64) (Workspace, error) {
	row := q.db.QueryRowContext(ctx, getWorkSpaceByOwnerUserID, ownerUserID)
	var i Workspace
	err := row.Scan(
		&i.ID,
		&i.OwnerUserID,
		&i.Name,
		&i.DefaultCurrency,
		&i.CreatedAt,
	)
	return i, err
}
